// Towers of Hanoi

class TowersOfHanoi {

  run() {
    // until first tower is empty, along with either the second or third
      // get move from player (move from tower and move to tower)
      // check if valid move:
        // larger disk cannot be moved on top of smaller disk
        // can't move from empty tower
      // if valid, make move
  }

}
